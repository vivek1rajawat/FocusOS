VITE_GOOGLE_CLIENT_ID=

# Base URL for the backend API, including the /api suffix, e.g.
# https://focusos-api.onrender.com/api. Leave unset for local dev — Vite proxies
# /api to localhost:5000 automatically (see vite.config.js).
VITE_API_URL=
